import { CartFill } from 'react-bootstrap-icons';

const CartWidget = () => {
    return (
        <div>
            <button><CartFill size={20} /></button>
            0
        </div>

    );
}

export default CartWidget;